export interface LoginRequestInterface {
    username: string;
    password: string;
}

export interface RegisterRequestInterface {
    username: string;
    password: string;
    email: string;
    birthDate: string;
    description: string;
}